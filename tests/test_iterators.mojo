from std.testing import assert_equal, assert_true

from playground.iterators import Reader, ReaderIter


def test_reader_iter() raises:
    # Test that Reader and ReaderIter can be imported
    # Full testing would require file I/O
    assert_true(True)


def main() raises:
    test_reader_iter()
