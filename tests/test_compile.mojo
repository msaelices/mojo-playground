from std.testing import assert_true

from playground.compile import add_fn, forloop_fn, print_fn


def test_compile_imports() raises:
    # Test that compile functions can be imported
    assert_true(True)


def main() raises:
    test_compile_imports()
