from std.testing import assert_true

from playground.strings import print_char, join_str


def test_strings_imports() raises:
    # Test that strings functions can be imported
    assert_true(True)


def main() raises:
    test_strings_imports()
